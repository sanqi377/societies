module.exports = {
  setting: {
    // mysql 配置
    mysql: {
      host: "localhost",
      user: "root",
      password: "root",
      database: "societies",
    },
    // server 配置
    server: {
      port: 3000
    }
  }
}