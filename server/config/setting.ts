module.exports = {
  setting: {
    // mysql 配置
    mysql: {
      host: "localhost",
      user: "societies",
      password: "societies",
      database: "societies",
    },
    // server 配置
    server: {
      port: 3000
    }
  }
}