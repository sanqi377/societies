<view class="container">
  <view class="item">
    <span class="icon">
      <image class="icons" src="../../public/icon/{{icon}}.png"></image>
    </span>
    <input class="input" type="{{type}}" placeholder="{{placeholder}}" value="{{val}}" bindinput="getVal" />
  </view>
</view>